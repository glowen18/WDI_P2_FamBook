<%= form_for ([@board, story]) do |f| %>
  <% if story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(instruction.errors.count, 'error') %> prohibited this story from being saved:</h2>

      <ul>
        <% story.errors.full_messages.each do |message| %>
          <li><%= message %></li>
       <% end %>
      </ul>
    </div>
  <% end %>

  <div class='field'>
    <%= f.label :date %>
    <%= f.date_select :date, :start_year => 1900, :end_year => Date.today.year %>
  </div>
  <div class='field'>
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class='field'>
    <%= f.label :description%>
    <%= f.text_field :description %>
  </div>

  <div class='actions'>
    <%= f.submit %>
  </div>
<% end %>
